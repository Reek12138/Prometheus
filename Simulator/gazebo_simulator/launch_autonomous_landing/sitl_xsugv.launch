<?xml version="1.0"?>
<launch>

  <!-- <include file="$(find gazebo_ros)/launch/empty_world.launch">
    <arg name="world_name" value="$(find xsugv_gazebo)/worlds/xsugv_playpen.world"/>
    <arg name="paused" value="false"/>
    <arg name="use_sim_time" value="true"/>
    <arg name="gui" value="true"/>
    <arg name="headless" value="false"/>
    <arg name="debug" value="false"/>
  </include> -->

     <!-- Load the custom empty world with friction -->
  <arg name="world_name" default="$(find prometheus_gazebo)/worlds/TD3.world"/>
  <!-- <arg name="world_name" default="$(find prometheus_gazebo)/worlds/prometheus_logo.world"/> -->

  <!-- Launch Gazebo with the custom empty world -->
  <include file="$(find gazebo_ros)/launch/empty_world.launch">
    <arg name="world_name" value="$(arg world_name)"/>
  </include>

  <include file="$(find xsugv_gazebo)/launch/spawn_xsugv.launch">
    
    <arg name="aruco" value="landing_pad"/>
    <arg name="x" value="0.0"/>
    <arg name="y" value="0.0"/>
    <arg name="z" value="0"/>
    <arg name="yaw" value="0"/>
  </include>

</launch>
